============
Installation
============

At the command line::

    $ pip install os-capabilities

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv os-capabilities
    $ pip install os-capabilities
